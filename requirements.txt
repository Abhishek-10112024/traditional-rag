# Core RAG runtime (used by src/, streamlit_app.py, main.py, scripts/ingest_documents.py)
sentence-transformers>=2.5.0
chromadb>=0.5.0
rank-bm25>=0.2.2
requests>=2.32.0
transformers>=4.36.0
torch>=2.0.0
streamlit>=1.32.0
python-dotenv>=1.0.0
numpy>=2.0.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
loguru>=0.7.2
tqdm>=4.66.0
pypdf>=4.2.0
python-docx>=1.1.0

# Evaluation (evaluation/evaluator.py, scripts/evaluate.py — RAGAS metrics)
# Remove these two lines only if install fails on your Python version and you do not need evaluation.
datasets>=2.14.0
ragas>=0.1.0
