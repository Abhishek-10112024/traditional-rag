# Core RAG Dependencies - Updated for Python 3.13
langchain>=0.3.0
langchain-community>=0.2.0
langchain-core>=0.3.0

# Embeddings & Vector Store
sentence-transformers>=2.5.0
chromadb>=0.5.0

# Note: FAISS binary wheels not available for Python 3.13
# Install via: pip install faiss-cpu --prefer-binary (may need to build from source)
# Or omit and use Chroma-only mode

# Search & Ranking
rank-bm25>=0.2.2
scikit-learn>=1.4.0

# LLM Integration
ollama>=0.3.0
huggingface-hub>=0.23.0
requests>=2.32.0
transformers>=4.36.0
torch>=2.0.0

# Evaluation (RAGAS - Optional)
# ragas and datasets have version issues with Python 3.13
# Install separately if needed: pip install ragas datasets --upgrade

# Web Framework
streamlit>=1.32.0
streamlit-chat>=0.1.1

# Utilities
python-dotenv>=1.0.0
numpy>=2.0.0
pandas>=2.2.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
tqdm>=4.66.0
loguru>=0.7.2

# Development
pytest>=7.4.0
pytest-cov>=4.1.0
black>=24.0.0
flake8>=7.0.0
